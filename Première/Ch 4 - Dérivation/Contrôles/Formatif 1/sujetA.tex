
\documentclass{exam}
\usepackage{tasks}

\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage{amsthm}
\usepackage[french]{babel}
\usepackage{enumitem}
\usepackage{currfile}

\usepackage{tikz}
\usetikzlibrary{calc}
\usepackage{qrcode}
\usepackage{mathrsfs}
\usepackage{enumitem}
\usepackage[french]{babel}
\usepackage{tcolorbox}
\setlength{\parindent}{0cm}

\usepackage{multicol}
\newenvironment{clist}{\begin{center}\begin{inparaenum}}{\end{inparaenum}\end{center}}

\pagestyle{empty}
\graphicspath{ {../../Images/} }
\input{def.tex}

\usepackage{geometry}
\geometry{left=1.5cm, right=1.5cm, top=1.5cm, bottom=1.5cm}

\makeatletter
\def\gap#1{%
  \setbox\@tempboxa\hbox{{\color{white}#1}}%
  \@tempdima\wd\@tempboxa
  \rlap{\unhbox\@tempboxa}%
  \hb@xt@\@tempdima{\dotfill}%
}
\makeatother   

\theoremstyle{definition}
\newtheorem{exercice}{Exercice}

\begin{document}
    \large

    \vfill

    \noindent{
    \makebox[0pt][l]{\textbf{\Niveau}}%
    \makebox[\textwidth][c]{\textbf{\ChapitreCourt}}%
    \makebox[0pt][r]{\textbf{\Document\ (A) -- 55min}}
    }

    \vspace{2mm}
    \noindent
    \makebox[\textwidth]{\textbf{Nom et prénom :}\enspace\hrulefill}

    %\noindent{\makebox[\textwidth]{Nom et prénom:\enspace\hrulefill}}

    \vspace{2mm}
    % Exercice 4, 11, 19, 24, 25, 26, 29
% % Exercice 1
% \begin{exercice}[title=Exercice 1 $\quad \star \star \quad$ { [Calculer, Raisonner] }]{}{}
% Soit $f$ la fonction définie sur $\mathbb{R}$ par $f(x) = x^2$.
% \begin{enumerate}
%     \item Montrer que $f$ est dérivable en $1$ et calculer $f'(1)$.
%     \item Montrer que $f$ est dérivable en $-2$ et calculer $f'(-2)$.
%     \item Tracer la courbe $C_f$ à la calculatrice et vérifier graphiquement les résultats des questions précédentes.
% \end{enumerate}
% \end{exercice}

% % Exercice 2
% \begin{exercice}[title=Exercice 2 $\quad \star \star \quad$ { [Calculer, Raisonner] }]{}{}
% Soit $f$ la fonction définie sur $\mathbb{R}$ par $f(x) = x^2 + 3x - 5$.
% \begin{enumerate}
%     \item Montrer que $f$ est dérivable en $-1$ et calculer $f'(-1)$.
%     \item Tracer la courbe $C_f$ à la calculatrice et vérifier graphiquement le résultat de la question précédente.
% \end{enumerate}
% \end{exercice}

% % Exercice 3
% \begin{exercice}[title=Exercice 3 $\quad \star \star \quad$ { [Calculer, Raisonner] }]{}{}
% Soit $f$ la fonction définie sur $\mathbb{R}^+$ par
% $$f(x) = \dfrac{3}{x + 1}$$
% \begin{enumerate}
%     \item Montrer que $f$ est dérivable en $3$ et calculer $f'(3)$.
%     \item Tracer la courbe $C_f$ à la calculatrice et vérifier graphiquement le résultat de la question précédente.
% \end{enumerate}
% \end{exercice}

% % Exercice 4
% \begin{exercice}[title=Exercice 4 $\quad \star \quad$ { [Représenter] }]{}{}
% Soit $f$ une fonction définie sur $\mathbb{R}$, dérivable en $a = 0$ et $a = 2$. On donne ci-dessous la courbe $C_f$ ainsi que la tangente à $C_f$ au point d'abscisse $0$ (en rouge) et la tangente à $C_f$ au point d'abscisse $2$ (en vert).
% \begin{center}
% \includegraphics[width=0.9\linewidth]{swappy-20241202_172204.png}
% \end{center}
% Déterminer graphiquement $f'(0)$ et $f'(2)$.
% \end{exercice}

% Exercice 5
\begin{exercice}{(5 min)}
Soit $f$ une fonction définie et dérivable sur $\mathbb{R}$. On donne ci-dessous la courbe $C_f$ ainsi que la tangente $T_1$ à $C_f$ au point d'abscisse $1$ (en orange) et la tangente $T_2$ à $C_f$ au point d'abscisse $2$ (en vert).
\begin{center}
\includegraphics[width=0.6\linewidth]{functionA}
\end{center}
Déterminer graphiquement les équations des tangentes $T_1$ et $T_2$ à $C_f$.
\end{exercice}

% % Exercice 6
% \begin{exercice}[title=Exercice 6 $\quad \star \quad$ { [Représenter] }]{}{}
% Soit $f$ une fonction définie sur $\mathbb{R}$, dérivable en $a = -1$ et $a = 1$. On donne ci-dessous la courbe $C_f$ ainsi que les tangentes à la courbe $C_f$ respectivement aux points d'abscisses $-2$, $0$ et $1$.
% \begin{center}
% \includegraphics[width=0.9\linewidth]{swappy-20241202_172226.png}
% \end{center}
% Déterminer graphiquement les équations des tangentes à la courbe $C_f$ respectivement aux points d'abscisses $-2$, $0$ et $1$.
% \end{exercice}

% % Exercice 7
% \begin{exercice}[title=Exercice 7 $\quad \star \quad$ { [Représenter] }]{}{}
% Pour chaque fonction, tracer la courbe $C_f$ à l'aide de la calculatrice puis déterminer graphiquement l'équation de la tangente à $C_f$ au point d'abscisse $a$.
% \begin{enumerate}
%     \item $f(x) = -x^3 + 5x + 7$ et $a = 2$,
%     \item $f(x) = \dfrac{x}{3 + 8x}$ et $a = 1$,
%     \item $f(x) = \sqrt{3x + 7}$ et $a = 3$,
%     \item $f(x) = \dfrac{2x + 7}{\sqrt{x}}$ et $a = 2$.
% \end{enumerate}
% \end{exercice}

% % Exercice 8
% \begin{exercice}[title=Exercice 8 $\quad \star \quad$ { [Calculer] }]{}{}
% Soit $f$ une fonction définie et dérivable sur $\mathbb{R}$ telle que $f(2) = 5$ et $f'(2) = -3$. Déterminer l'équation de la tangente à la courbe $C_f$ au point d'abscisse $2$.
% \end{exercice}

% % Exercice 9
% \begin{exercice}[title=Exercice 9 $\quad \star \quad$ { [Calculer] }]{}{}
% Soit la fonction $f$ définie sur $\mathbb{R}_+^*$ par $f(x) = \sqrt{x}$. On admet que $f$ est dérivable en $2$ et que $f'(2) = \dfrac{1}{4}$. Déterminer l'équation de la tangente à $C_f$ au point d'abscisse $2$.
% \end{exercice}

% % Exercice 10
% \begin{exercice}[title=Exercice 10 $\quad \star \star \quad$ { [Calculer, Représenter] }]{}{}
% Soit la fonction $f$ définie sur $\mathbb{R}_+^*$ par $f(x) = \dfrac{1}{x}$.
% \begin{enumerate}
%     \item En calculant le taux de variation, montrer que $f$ est dérivable en $1$ et calculer $f'(1)$.
%     \item En déduire l'équation de la tangente $T_1$ à $C_f$ au point d'abscisse $1$.
%     \item Étudier la position relative de la tangente $T_1$ par rapport à $C_f$.
%     \item En procédant comme précédemment, déterminer l'équation de la tangente $T_{-1}$ à $C_f$ au point d'abscisse $-1$. Que peut-on dire des droites $T_1$ et $T_{-1}$ ?
% \end{enumerate}
% \end{exercice}

% % Exercice 11
% \begin{exercice}[title=Exercice 11 $\quad \star \star \quad$ { [Représenter] }]{}{}
% On considère une fonction $f$ définie sur $[-3; 3]$ et dérivable pour tout réel $x$ de l’intervalle $[-3; 3]$ dont on donne le tableau de valeurs suivant :
% 
% \begin{equation*}\arraycolsep=3pt\def\arraystretch{2}
% \begin{array}{|c|c|c|c|c|c|c|}
%     \hline
%     x & -3 & -1 & 0.5 & 2 & 3 \\
%     \hline
%     f(x) & 0 & 1 & 0.5 & 3 & -2 \\
%     \hline
%     f'(x) & 2 & 0 & -1 & -1 & 0 \\
%     \hline
% \end{array}
% \end{equation*}
% Dessiner avec le plus de précision possible la courbe $\mathbf{C}(f)$ sur l'intervalle $[-3; 3]$.
% \end{exercice}

% Exercice 12
\begin{exercice}{(10 min)}
    Pour chaque fonction $f$, justifier qu’elle est dérivable en $a$ et déterminer l’équation de la tangente à $C_f$ au point d’abscisse $a$.
    \begin{enumerate}
        % \item $f(x) = \dfrac{1}{x}$ et $a = -2$
        \item $f(x) = \sqrt{x}$ et $a = 4$
        \item $f(x) = x^4$ et $a = 3$
        % \item $f(x) = \sqrt{x}$ et $a = 3$
        % \item $f(x) = \dfrac{1}{x}$ et $a = -6$
        % \item $f(x) = x^3$ et $a = 7$
    \end{enumerate}
    \end{exercice}
    
    % Exercice 13
    \begin{exercice}{(15 min)}
    Pour chaque fonction $f$, déterminer son ensemble de définition, son ensemble de dérivabilité en justifiant, puis calculer $f'(x)$ en simplifiant au maximum.
    \begin{enumerate}
        %\item $f(x) = 2x^2 - x$
        \item $f(x) = \dfrac{x^3}{3} + \dfrac{3}{x}$
        %\item $f(x) = \dfrac{3\sqrt{x}+1}{5}$
        \item $f(x) = \dfrac{x}{\sqrt{x} - 1}$
        \item $f(x) = \sqrt{2x - 4}$
        % \item $f(x) = -5x^2 + 5$
        % \item $f(x) = \dfrac{5}{x} - \dfrac{x^4}{4}$
        %\item $f(x) = \dfrac{2x^3-1}{5}$
        %\item $f(x) = \dfrac{\sqrt{x}}{x + 1}$
        %\item $f(x) = \sqrt{-2x + 1}$
    \end{enumerate}
    \end{exercice}
    
%     % Exercice 16
%     \begin{exercice}[title=Exercice 16 $\quad \star \star \quad$ { [Représenter, Calculer] }]{}{}
%     Soit $f$ une fonction polynôme du second degré définie par $f(x) = ax^2 + bx + c$ (avec $a \neq 0$). On note $C_f$ sa courbe représentative dans un repère. On considère par ailleurs deux points $A$ et $B$ de $C_f$ d’abscisses respectives $\alpha$ et $\beta$ telles que $\alpha \neq \beta$.
%     \begin{enumerate}
%         \item[(a)] Démontrer que l’équation de la tangente à $C_f$ au point $A$ est :
%         \[
%         y = (2a\alpha + b)x + c - a\alpha^2.
%         \]
%         \item[(b)] Déterminer de même l’équation de la tangente à $C_f$ au point $B$.
%         \item Démontrer que ces tangentes se coupent en un point d’abscisse $\frac{\alpha + \beta}{2}$.
%         \item Vérifier que :
%         \[
%         f(\beta) - f(\alpha) = (\beta - \alpha) f'\left(\frac{\alpha + \beta}{2}\right).
%         \]
%         \item En déduire que la tangente à $C_f$ au point d’abscisse $\dfrac{\alpha + \beta}{2}$ est parallèle à la droite $(AB)$.
%         \item Faire une figure illustrant le résultat démontré dans cet exercice.
%     \end{enumerate}
%     \end{exercice}    

    % Exercice 17
\begin{exercice}{(15 min)}
    Soit $f$ la fonction définie sur $\mathbb{R}^+$ par $f(x) = (x-1)\sqrt{x-1}$.
    \begin{enumerate}
        \item Justifier que $f$ est dérivable sur $\left]-1; +\infty\right[$ et calculer $f'(x)$ pour tout $x \in \left]-1; +\infty\right[$.
        \item En calculant la limite du taux d'accroissement, montrer que $f$ est dérivable en $-1$ puis en déduire $f'(-1)$.
        %\item D’après le cours, si $f$ et $g$ sont deux fonctions dérivables en $a$, alors $f \times g$ est dérivable en $a$. La réciproque de cette propriété est-elle vraie ? Justifier.
    \end{enumerate}
    \end{exercice}
    
    % % Exercice 18
    % \begin{exercice}[title=Exercice 18 $\quad \star \star \star \quad$ { [Calculer, Raisonner] }]{}{}
    % Soit $f$ la fonction définie sur $\mathbb{R}$ par $f(x) = |x + 6|$. Étudier la dérivabilité de $f$ en tout point de $\mathbb{R}$.
    % \end{exercice}
    
    % % Exercice 19
    % \begin{exercice}[title=Exercice 19 $\quad \star \star \star \quad$ { [Calculer, Représenter] }]{}{}
    % On considère la fonction $f$ définie sur $\mathbb{R}$ par $f(x) = x^3 + x^2 - 7x - 4$. La courbe représentative de $f$ admet-elle des tangentes parallèles à la droite d’équation $y = x + 4$ ? Si oui, préciser en quel(s) point(s).
    % \end{exercice}
    
    % % Exercice 20
    % \begin{exercice}[title=Exercice 20 $\quad \star \star \star \quad$ { [Calculer, Raisonner] }]{}{}
    % Soit $f$ la fonction définie sur $\mathbb{R}$ par \\$f(x) = x^2 - 5x + 6$.
    % \begin{enumerate}
    %     \item Factoriser le polynôme $f(x)$.
    %     \item On définit, pour tout $x \in \mathbb{R}$, $$g(x) = |f(x)|.$$
    %     \begin{enumerate}
    %         \item Justifier que $g$ est dérivable sur $]-\infty; -3[\ \cup\ ]2; +\infty[$ puis calculer $g'(x)$ pour tout $x$ appartenant à cet intervalle.
    %         \item Justifier que $g$ est dérivable sur $]-3; 2[$ puis calculer $g'(x)$ pour tout $x$ appartenant à cet intervalle.
    %         \item Étudier la dérivabilité de $g$ en $-3$ et en $2$.
    %     \end{enumerate}
    % \end{enumerate}
    % \end{exercice}
    
    % % Exercice 21
    % \begin{exercice}[title=Exercice 21 $\quad \star \star \star \quad$ { [Chercher, Calculer, Représenter] }]{}{}
    % On note $f$ la fonction carrée et $g$ la fonction inverse. Déterminer les tangentes communes aux courbes $C_f$ et $C_g$.
    % \end{exercice}
    
    \begin{tcolorbox}
        \Large
        \begin{center}
            \underline{Attention :}
        \end{center}
        \vspace{-0.5em}
        Indiquez la version (A ou B) sur votre copie. \underline{Joignez-y votre sujet} en ayant renseigné vos \underline{nom et prénom}.\\
        Tout \underline{oubli} pourra être considéré comme une \underline{tentative de triche}.
    \end{tcolorbox}

\newpage

\begin{center}
    \LARGE
    \textbf{\underline{Corrigé}}
\end{center}
\setcounter{exercice}{0}

\begin{exercice}
    \begin{enumerate}
        \item[]
        \item $T_1$ a pour équation $y = -1$:\\
        \\
        \underline{Justification :}\\
        $T_1$ est horizontale donc son coefficient directeur est $0$.\\
        On a donc $T_1: y = 0x + b$.\\
        Comme tout point de $T_1$ (ex: $A(0; -1)$) a pour ordonnée $-1$ on a $b = -1$, en effet pour $A$:
        $$
            T_1(0) = -1 \iff b = -1
        $$
        Donc $T_1: y = -1$.
        \item $T_2$ a pour équation $y = -2x + 4$:\\
        \\
        \underline{Justification :}\\
        En prenant deux points par exemple $A(\frac{3}{2}; -1)$ et $B(2; \frac{1}{2})$ on calcule le coefficient directeur:
        $$
            \frac{y_B - y_A}{x_B - x_A} = \frac{\frac{1}{2} - (-1)}{2 - \frac{3}{2}} = \frac{1 + 2}{4 - 3} = 3
        $$
        Donc le coefficient directeur de $T_2$ est $f'(2) = 3$.\\
        L'équation de $T_2$ est donc $f'(2)(x-2) + f(2)$. Par lecture on trouve $f(2) = \frac{1}{2}$.
        Donc $T_2: y = 3(x-2) + \frac{1}{2} = 3x - \frac{11}{2}$.
    \end{enumerate}
\end{exercice}

\begin{exercice}
    \begin{enumerate}
        \item[]
        \item L'ensemble de dérivabilité de $f(x) = \sqrt{x}$ est $\mathbb{R}_+$. Comme $a = 4 \in \mathbb{R}_+$, $f$ est bien dérivable en $a$.\\
        $f$ est la fonction racine et a donc pour dérivée:
        $$
            f'(x) = \frac{1}{2\sqrt{x}}
        $$
        Donc
        \begin{align*}
            f'(4) &= \frac{1}{2\sqrt{4}}\\
            &= \frac{1}{4}
        \end{align*}
        L'équation de la tangente est donc:
        \begin{align*}
            T_4:\ & y = \frac{1}{4}(x - 4) + f(4)\\
            & y = \frac{x}{4} - 1 + \sqrt{4}\\
            & y = \frac{x}{4} + 1
        \end{align*}
        \item L'ensemble de dérivabilité de $f(x) = x^4$ est $\mathbb{R}$. Comme $a = 4 \in \mathbb{R}$, $f$ est bien dérivable en $a$.\\
        $f$ est la fonction puissance et a donc pour dérivée:
        $$
            f'(x) = 4x^3
        $$
        Donc
        \begin{align*}
            f'(3) &= 4\times3^3\\
            &= 4\times27\\
            &= 108
        \end{align*}
        L'équation de la tangente est donc:
        \begin{align*}
            T_3:\ & y = 108(x - 3) + f(3)\\
            & y = 108x - 324 + 3^4\\
            & y = 108x - 243
        \end{align*}
    \end{enumerate}
\end{exercice}

\begin{exercice}
    \begin{enumerate}
        \item[]
        \item Considérons les différents termes de la fonction:
        \begin{itemize}
            \item $x^3$ est définie et dérivable sur tout $\mathbb{R}$ donc c'est aussi le cas pour $\frac{x^3}{3}$.
            \item $\frac{1}{x}$ n'est ni définie ni dérivable en $0$, ce qui est aussi le cas pour $\frac{3}{x}$.
        \end{itemize}
        Donc $f$ est définie et dérivable sur $\mathbb{R}^\star$.\\
        Calculons sa dérivée:
        \begin{align*}
            f'(x) &= \left(\frac{x^3}{3} + \frac{3}{x}\right)'\\
            &= \left(\frac{x^3}{3}\right)' + \left(\frac{3}{x}\right)' \qquad \text{(somme de fonction $(u+v)' = u' + v'$)}\\
            &= \frac{1}{3}\left(x^3\right)' + 3\left(\frac{1}{x}\right)' \qquad \text{(produit par une constante $(\lambda u)' = \lambda u')$}\\
            &= \frac{1}{3}\times 3x^2 + 3 \left(-\frac{1}{x^2}\right)\\
            &= x^2 - \frac{3}{x^2}
        \end{align*}
        \item Considérons les différents termes de la fonction:
        \begin{itemize}
            \item On a $\sqrt{x}$ dans la fonction qui est définie sur $\mathbb{R}_+$ et dérivable sur $\mathbb{R}_+^\star$.
            \item On a $\sqrt{x} - 1$ au dénominateur. On doit donc avoir $\sqrt{x} - 1 \neq 0$, soit $x \neq 1$.
        \end{itemize}
        Donc $f$ est définie sur $\mathbb{R}_+ \backslash \{ 1 \}$ et dérivable sur $\mathbb{R}_+^\star \backslash \{ 1 \}$ (ou $\mathbb{R}_+ \backslash \{ 0; 1 \}$).\\
        Pour calculer sa dérivée on observe que:
        $$
        f(x) = \frac{u}{v}
        $$
        avec
        \begin{align*}
        \begin{cases}
            u = x & u' = 1\\
            v = \sqrt{x} - 1 & v' = \frac{1}{2\sqrt{x}}
        \end{cases}
        \end{align*}
        Sa dérivée est donc:
        \begin{align*}
            f'(x) &= \frac{u'v - uv'}{v^2} \qquad \text{(produit de deux fonctions $(uv)'$)}\\
            &= \frac{1(\sqrt{x} - 1) - x(\frac{1}{2\sqrt{x}})}{(\sqrt{x} - 1)^2}\\
            &= \frac{\sqrt{x} - 1 - \frac{x}{2\sqrt{x}}}{(\sqrt{x} - 1)^2}\\
            &= \frac{\sqrt{x} - 1 - \frac{\sqrt{x}}{2}}{(\sqrt{x} - 1)^2}\\
            &= \frac{\frac{\sqrt{x}}{2} - 1}{(\sqrt{x} - 1)^2}\\
            &= \frac{\sqrt{x} - 2}{2(\sqrt{x} - 1)^2}
        \end{align*}
        \item Observons que la fonction $f(x) = \sqrt{2x - 4}$ est composée: $f(x) = g(2x - 4)$ avec $g(x) = \sqrt{x}$.\\
        Comme la fonction racine est définie sur $\mathbb{R}_+$ et dérivable sur $\mathbb{R}_+^\star$. Il faut:
        \begin{itemize}
            \item Pour la définition: $2x - 4 \in \mathbb{R}_+$, soit $2x - 4 \geq 0$ et donc $x \in \left[2; +\infty\right[$.\\ Donc $f$ est définie sur $\left[2; +\infty\right[$.
            \item Pour la dérivabilité: if faut $2x - 4 \in \mathbb{R}_+^\star$, soit $2x - 4 > 0$.\\
            De la même manière on trouve que $f$ est dérivable sur $\left]2; +\infty\right[$ (cette fois on exclut $0$).
        \end{itemize}
        Calculons sa dérivée:
        \begin{align*}
            f'(x) &= a \times g'(ax + b) \qquad \text{(fonction composée)}\\
            &= 2 \times \dfrac{1}{2\sqrt{2x-4}}\\
            &= \dfrac{1}{\sqrt{2x - 4}}
        \end{align*}
    \end{enumerate}
\end{exercice}

\begin{exercice}
    \begin{enumerate}
        \item[]
        \item Observons que $f(x)$ est le produit de deux fonctions:
        \begin{itemize}
            \item $(x-1)$ qui est définie et dérivable sur tout $\mathbb{R}$,
            \item $\sqrt{x-1}$ qui est définie pour $x - 1 \geq 0$ soit $x \geq 1$ et dérivable pour $x - 1 > 0$ soit $x > 1$.
        \end{itemize}
        Donc $f$ est définie sur $\left[1; +\infty\right[$ (déjà donné dans l'énoncé) et dérivable sur $\left]1; +\infty\right[$.
        \item Exprimons le taux d'accroissement en $1$:
        \begin{align*}
            \frac{f(1+h) - f(1)}{h} &= \frac{\left[(1+h)-1\right]\left(\sqrt{(1 + h) - 1}\right) - \left[(1 - 1)(\sqrt{1 - 1})\right]}{h}\\
            &= \frac{h\sqrt{h}}{h}\\
            &= \sqrt{h}
        \end{align*}
        En prenant la limite pour $h \rightarrow 0$ on trouve $f'(1) = 0$.\\
        Ainsi, la dérivée de $f$ est définie en $1$, alors $f$ est dérivable en $1$.\\
        Donc $f$ est dérivable sur $\left[1; +\infty\right[$.
    \end{enumerate}
\end{exercice}

\end{document}
